[package]
name = "parsley"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[build-dependencies]
# No build-time compiler dependency - uses QUICHE_COMPILER_BIN at build time

[dependencies]
quiche-runtime = { path = "../quiche-runtime" }

[lib]
path = "src/lib.rs"
