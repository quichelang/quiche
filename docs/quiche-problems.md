
## 13. `println` Not a Recognized Builtin

**Status: Won't fix** — `print()` is the canonical way. No alias needed.

---

## 15. Lambda Syntax Not Supported in Parser

**Status: Won't fix** — pipe syntax `|x: i64| x + 1` works. No Python `lambda` alias needed.
