# Args Test
# Note: This tests compilation of args, but runtime values depend on CLI input

def main_args(args: Vec[String]):
    print("=== Args Test ===")
    print("Received args:")
    # We can't easily assert strict values without a test runner passing them
    # But we can verify the code compiles and runs
    if len(args) > 0:
        print(args[0])
    
    # Just verify we can iterate them
    for arg in args:
        print(arg)
        
    print("=== Done ===")

def main():
    # Helper wrapper to simulate args if run without them, 
    # but the real signature test is `main(args)` support in parser.
    # Our compiler currently detects `main` vs `main(args)`.
    # To test `main(args)`, we need to actually define it as such.
    # But Quiche only supports one main per file.
    
    # We will simulate a call:
    args: Vec[String] = ["arg1", "arg2"]
    main_args(args)
