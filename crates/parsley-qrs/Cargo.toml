[package]
name = "parsley-qrs"
version = "0.1.0"
edition = "2024"

[build-dependencies]
# No build-time compiler dependency - uses QUICHE_BOOTSTRAP_BIN at build time

[dependencies]
quiche-runtime = { path = "../quiche-runtime" }

[lib]
path = "src/lib.rs"
