
def main():
    # Test Tuple creation
    # We expect this to map to Rust: let t: (i32, f64) = (10, 3.14);
    t: Tuple[i32, f64] = (10, 3.14)
    print(t)
    print(t[1])

    # Test nested tuple
    nested: Tuple[i32, Tuple[i32, i32]] = (1, (2, 3))
    print(nested)
    print(nested[1][0])
