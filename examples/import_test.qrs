from std.collections import HashMap
import std.vec

def main():
    # Use imported HashMap
    d: HashMap[i32, i32] = {1: 1}
    print(d[1])
    
    # Use qualified name (if imported)
    # create a vec manually (std.vec is just a dummy import here for testing)
    v: Vec[i32] = [1, 2, 3]
    print(v)
